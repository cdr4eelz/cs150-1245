XCO_SRC = $(wildcard *.xco)
V_SRC := $(patsubst %.xco,%.v,$(XCO_SRC))

all: $(V_SRC)

%.v: %.xco
	coregen -p . -intstyle silent -b $*.xco

clean:
	find . ! -name '*.xco' -a ! -name '*.coe' -a ! -name 'coregen.cgp' -a ! -name 'Makefile' -a ! -name '.gitignore' -a ! -name '*.bz2' -delete
