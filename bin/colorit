#!/bin/bash

STYLE=""
SS="color${STYLE}"
SEDOPT="$*"
#-n (to not auto-print)

SDIR=`dirname $0`
SS1="${SDIR}/${SS}_line.sed"
SS2="${SDIR}/${SS}_word.sed"

#while IFS= read -r LINE; do
#  echo $LINE
#done |
sed -f "${SS1}" |sed ${SEDOPT} -f "${SS2}"
