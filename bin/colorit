#!/bin/bash

STYLE="all"
SS="color_${STYLE}"
SEDOPT="$@"
#-n (to only print important matches)

SDIR=`dirname $0`
SS1="${SDIR}/${SS}_1.sed"
SS2="${SDIR}/${SS}_2.sed"

#while IFS= read -r LINE; do
#  echo $LINE
#done |
sed -f "${SS1}" |sed ${SEDOPT} -f "${SS2}"
