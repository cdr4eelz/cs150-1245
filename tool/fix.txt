KEYtag: BUG!/? STEP OPTImize TOOL CLeanUP ANALyze COOLcrossuse WANTtolearn

BUG!    Execute code via I-cache flakey (maybe but push/pop w/stall interaction?)
BUG-    Reset is double applied with 2-cycle gap (init_done lag)
BUG-    Timing errors (try separate PLLs, single-clock DDR, pull from sysgen, etc.)

CLUP    Revert ml505top as much as possible, move fanciness to sub/wrapper
CLUP    Re-group/balance modules
CLUP    Data vs control segregation
CLUP    OPCODE (& almost everything) as parameters rather than `define
CLUP    Top level `define -> localparam mappings

STEP    More thorough instruction/asm/C tests (partly started)
STEP    COP0 complete & test
STEP    ISR software
STEP    PixelFeeder from DDR (optional cross-clock count-estimate)
STEP    Reset deassert per-clock (and delay based on component)

TOOL    Use speedy distmem and custom instructions for special internal debug data/triggers
TOOL    Helper functions in scratch IMEM (link with smaller user code)
TOOL    Cache-Flush, Break, Scope-trigger, etc. instructions (for testing/debugging)
COOL    FSL link & instuctions (drop-in of/as uBlaze coprocs & use RVA tricks)
COOL    PixelFeeder as FSL (more cross-use even FIR with MIPS?)

WRAP    Retro-patch prior checkpoints?
WRAP    Writeup highlighting cross-project & extra-credit work
WRAP    Prune/cherry-pick git repo

ANAL    Track down flop styling (CE, SET/RESET, etc)
ANAL    Any "gated" clocks vs. clock-enable?
OPTI    Avoid If based priority logic (maybe even for Reset signals?) flatten & muxlike
OPTI    Cautious resource sharing?

WANT    Figure out Post-Synthesis Simulation
WANT    Use FPGAEditor and LOC/RLOC tricks in select spots
WANT    Rebuild custom GCC tools (without clang, with clang)
WANT    Try simple dual-processing arrangement

OLD?    ???Don't use OUTPUT as internal signal too???


::EXTRAS::
    Memory patching with .bmm file tricks
    SCRATCHPAD memory kept active
    BIOS "cp", "dump"
    PLOP for serial forwarding & other tricks
    BRKTAP for debug stepping & monitoring
    Research (revisions?) of timing, MIG details, uBlaze, etc.


::TROUBLESHOOT STEPS (when sim/synth discrep)::
    * Reverted to sparse CP2 top & framework *
    * Verified all functions are combinational only *
    * Reviewed CASE statements (confirmed no caseX/caseZ) *
    * Attempted fresh MIG generation with ChipScope access *
    * Spot out-of-order assingments to temp variables in always@* blocks.
    * Locate all missing signals!!!


::LESSONS::
    Questionable tricks (short-term not worth long-term)
    Control/Datapath useful (though difficult for big pipeline CPU)
    
