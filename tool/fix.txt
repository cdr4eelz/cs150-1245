::PRIORITIES::
    More thorough instruction/asm/C tests
    COP0 complete & test
    ISR software
    PixelFeeder from DDR (optional cross-clock count-estimate)
    Reset deassert per-clock (and delay based on component)
    FSL links & commands (allow drop-in of uBlaze coprocs)

::LIKELY BUGS::
    Reworked fetch still skipping instruction after stall.
    ? Bios "cp" to 0x3 I/D-caches seems to lockup
    ? First run of code in I-Cache is flakey
        Seems to run multiple times, maybe "conditional" value/test failing...
        ...perhaps just the saving of registers to memory is messed.
        Likely stall bugs (maybe misunderstanding about dual-write)
    ? First xmit when running from I-Cache seems to get lost/corrupt:
        Maybe stall not handled properly, maybe just part of flakey DDR run above
    ? Can_Forward during stall (don't change based on stall)
    EchoTestBench doesn't run echo.
    Reset is double applied with 2-cycle gap (init_done lag)

::TOOLS::
    Cache-Flush, Break, Scope-trigger, etc. instructions (for testing/debugging).
    Archive of code in scratch IMEM ("cp" or hw-copy to DDR to fetch)
        (If auto-copy to DDR on boot, maybe could simulate run in DDR)
    Put common routines at steady "bios" entry-points for user-program use.


::SPECIAL TROUBLESHOOT STEPS::
* Reverted to sparse CP2 top & framework *
* Verified all functions are combinational only *
* Reviewed CASE statements (confirmed no caseX/caseZ) *
* Attempted fresh MIG generation with ChipScope access *
* Spot out-of-order assingments to temp variables in always@* blocks.
* Locate all missing signals!!!

WANT: Figure out Post-Synthesis Simulation.


Gated clocks.
Don't use OUTPUT as internal signal too.
Avoid If based priority logic (maybe even for Reset signals?) flatten & muxlike.

BUS abstraction (mimick xps?)
OPCODE as parameters rather than `define's
Cautious resource sharing.
