
SUBS ?= $(shell find * -mindepth 1 -maxdepth 1 -name "Makefile" -printf '%h ')

.PHONY: all core tidy clean $(SUBS)

all core tidy clean: $(SUBS)
	@echo "DONE: $@ ($^)"

$(SUBS):
	@echo ""
	$(MAKE) --no-builtin-rules -C $@ $(MAKECMDGOALS)
	@echo ""
